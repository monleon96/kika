#
# Local data generated by the Streamlit UI (SQLite database, etc.)
#

# SQLite database storing accounts and user preferences
kika.db


# Deprecated SQLite database (replaced by kika-backend PostgreSQL)
kika.db
