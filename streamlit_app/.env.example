# Environment Configuration for KIKA Streamlit App

# Backend API Configuration
# URL of the kika-backend FastAPI server
# Default: http://localhost:8000
KIKA_BACKEND_URL=http://localhost:8000

# Admin API Key (optional)
# Required for admin operations (create/deactivate users, list users)
# Must match the ADMIN_API_KEY in kika-backend .env
# KIKA_ADMIN_KEY=your-admin-key-here

# Legacy Configuration (no longer used)
# The following were used by the old embedded authentication system:
# - KIKA_SECRET_KEY (now handled by backend JWT_SECRET)
# - KIKA_PUBLIC_BASE_URL (now handled by backend PUBLIC_BASE_URL)
# - KIKA_SMTP_* (now handled by backend SMTP settings)
# - KIKA_DB_PATH (SQLite no longer used)
